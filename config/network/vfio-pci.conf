# VFIO-PCI Konfiguration für SFP+ Passthrough
# Nur pfSense Port: 0c:00.1 (sfp9) via echo ins sysfs
# Host-Ports: 0b:00.0 (sfp6), 0b:00.1 (sfp7), 0c:00.0 (sfp8) bleiben beim ixgbe Treiber

# Keine automatische Device-ID Bindung hier - wird über Skript gemacht
# Host-Ports: 0b:00.0 (sfp6), 0b:00.1 (sfp7), 0c:00.0 (sfp8) bleiben beim ixgbe Treiber